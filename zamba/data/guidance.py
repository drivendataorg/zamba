

from typing_extensions import ParamSpecArgs
import subprocess

def default_parameter_schedule():
    pass


def guidance(video_loader_config, train_config, parameter_variation):
    pass


def run_and_catch(config):
    # write config to tmp

    # run as subprocess

    # killed for OOM
    # OOM error
    # CUDA Memory error
    
    # per-batch timings, no cache
    # per-batch timings, now cached